# demo-app

A deliberately small fixture project for the bioprism-project scanner: one pinned
dependency, one unpinned dependency, one marker in `src/main.rs`, one test in
`src/lib.rs`, one CI workflow, and one non-UTF-8 asset.
