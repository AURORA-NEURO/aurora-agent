[package]
name = "bioprism-worldgen"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Synthetic structural benchmark families: worlds whose topology, depth and tag informativeness vary independently"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# Tests and the scaling example only. The generator emits world and query *documents*; loading one
# back is how the tests check it is well formed, and is not something the library itself does.
bioprism-world = { workspace = true }
bioprism-baseline = { workspace = true }
bioprism-section = { workspace = true }
bioprism-fiber = { workspace = true }
