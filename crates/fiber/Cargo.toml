[package]
name = "bioprism-fiber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "The FIBER query compiler: protected closure, dependency slicing, temporal cut and certificate emission"

[dependencies]
bioprism-ids = { workspace = true }
bioprism-scope = { workspace = true }
bioprism-section = { workspace = true }
bioprism-world = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Tests only. The policy pass is classified by bioprism-governance rather than by assertion, and
# the crate that owns the rule is the one that must answer.
bioprism-governance = { workspace = true }
