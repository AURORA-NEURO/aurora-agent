Compile the reference decision context in the aurora-agent checkout (world
`fixtures/fiber-v0.1/radiogenomic_world.json`, query
`fixtures/fiber-v0.1/leakage_query.json`) and report: the oracle status, every
witness by name, how many facts were selected vs omitted, and the first 8
characters of the certificate digest.
