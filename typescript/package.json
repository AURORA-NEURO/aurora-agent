{
  "name": "@aurora-neuro/prism-sdk",
  "version": "0.1.0",
  "description": "Dependency-free TypeScript client for the bounded AURORA Prism API",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
